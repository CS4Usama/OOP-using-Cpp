#include <iostream>
#include <conio.h>
#include <math.h>
using namespace std;
int main()
{
	int a, b;
	cout<<"Enter First Integer: ";
	cin>>a;
	cout<<"Enter Second Integer: ";
	cin>>b;
	cout<<"The Result of pow(a,b) = "<<pow(a,b);
	return 0;
}
